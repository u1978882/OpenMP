#!/bin/bash
gcc -fopenmp -o codi $1
echo "---------------------------------------------------------"
echo "| Executant" $1 "-" $3 "iteracions |"
echo "---------------------------------------------------------"
for (( c=1; c<=$3; c++ ))
do
  echo $(./codi $2)
done
rm codi
echo "---------------------------------------------------------"
